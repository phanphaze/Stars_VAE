#  To install new packages, use command: conda env update --file environment.yml --prune
name: sse_vae_env
channels:
  - defaults
dependencies:
  - python=3.10
  - numpy
  - pandas
  - matplotlib
  - jupyter
  - pip
  - pip:
    - --extra-index-url https://download.pytorch.org/whl/cu126
    - torch
    - torchvision
